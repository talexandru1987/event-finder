{{#if isLoggedIn }}
  {{> privateNavBar }}
{{else}}
  {{> publicNavBar }}
{{/if}}
<main id="main">
<div><h1>Events in your area</h1></div>
<!-- events cards -->
<div class="searchEventsTable" id="events-container" data-search-key="{{searchKey}}">
  {{#each events}}
    {{> eventCard}}
  {{/each}}
</div>
</main>

  

